package com.sps.sns.config;

// RestTemplateConfig eliminado: el SNS mock ya no hace callbacks HTTP al purchase-service.
// El purchase-service procesa el resultado directamente en su cadena WebFlux reactiva.
// Este archivo se conserva vacio para no romper la estructura del paquete.
